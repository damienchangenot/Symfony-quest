{% extends 'base.html.twig' %}

{% block title %}Edit Actor{% endblock %}

{% block body %}
    <h1>Edit Actor</h1>
    <div class="container">
    {{ include('actor/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('actors_admin_index') }}">back to list</a>

    {{ include('actor/_delete_form.html.twig') }}
    </div>
{% endblock %}
