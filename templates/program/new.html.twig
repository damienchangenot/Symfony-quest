{% extends "base.html.twig" %}

{% block body %}
    <div class="container d-flex flex-column text-center  align-items-center mt-5">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type='submit' value='Add program'>
    {{ form_end(form) }}
    </div>
{% endblock %}